// primitives
// complex
// objects
// arrays methods
// callbacks
// promises
// async await
// classes
// DOM
// DOM manipulation
// events

const obj = { size: 12, width: 22 };

const getParam = (obj, prop) => {
  return obj[property];
};

console.log(getParam(obj));
