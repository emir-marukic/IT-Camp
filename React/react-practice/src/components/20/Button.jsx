const Button = () => {
  return (
    <button
      style={{
        background: "blue",
        padding: "10px 20px",
        color: "#fff",
        borderRadius: "0.25rem",
        border: "none",
      }}
    >
      Button
    </button>
  );
};

export default Button;
